@model IEnumerable<WorkExperiences>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Work Experience List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="WorkExperience" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create new work experience
            </a>

        </div>
    </div>

    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    Title Name
                </th>
                <th>
                    Company Name
                </th>
                <th>
                    Start Date
                </th>
                <th>
                    End Date
                </th>
                <th>
                    Responsibilities
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td width="12%">
                        @obj.Title
                    </td>
                    <td width="12%">
                        @obj.Company
                    </td>
                    <td width="12%">
                        @obj.StartDate
                    </td>
                    <td width="12%">
                        @obj.EndDate
                    </td>
                    <td width="12%">
                        @obj.Responsibilities
                    </td>
                    
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="WorkExperience" asp-action="Edit" asp-route-id="@obj.Id"
                               class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Edit</a>
                            <a asp-controller="WorkExperience" asp-action="Delete" asp-route-id="@obj.Id"
                               class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Delete</a>
                        </div>
                    </td>

                </tr>
            }
        </tbody>
    </table>
</div>